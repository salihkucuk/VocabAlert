import SwiftUI

struct LoadingView: View {
    var body: some View {
        // You can customize this view
        VStack {
            Text("Loading...")
            ProgressView()
        }
    }
}
